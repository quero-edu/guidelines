# `@quero/eslint-config-nuxt`

You only need to install `elsint` and this package;

```base
pnpm add -D eslint @quero/elsint-config-nuxt
# or
npm install --save-dev eslint @quero/elsint-config-nuxt
```

```javascript
module.exports = {
  extends: ['@quero/eslint-config-nuxt'],
}
```
